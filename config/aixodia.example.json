{
  "_comment": "Runtime config for the AIxodia daemon/bridge. Copy to config/aixodia.json (gitignored) and edit. This file holds NO credential: the only token in the system is the D1 access token, typed by the operator in the app and handed to the daemon in the WebSocket Authorization header.",

  "worker_base": "https://aixodia.<your-subdomain>.workers.dev",

  "mobile_ws": {
    "listen": "127.0.0.1:18789",
    "public_listen": "0.0.0.0:18789",
    "tunnel": true,
    "cloudflared": "cloudflared"
  },

  "state_keys": {
    "provider_config": "config/provider",
    "system_config": "config/system",
    "entry_config": "config/entry",
    "session_prefix": "sessions/"
  }
}
