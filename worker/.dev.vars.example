# Copy to .dev.vars (gitignored) for `wrangler dev` on this machine.
# .dev.vars is the ONLY place this token may live outside the Worker secret store.
AIXODIA_TOKEN=change-me
# AI_DAEMON_WS=wss://127.0.0.1:18789/ws
