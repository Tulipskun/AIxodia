.gradle/
local.properties
.idea/
*.iml
.DS_Store
build/
app/build/
app/*.keystore
app/aixodia-debug.keystore.b64
*.jks
local.env.json

# local mock stack data (acts as the DB while testing)
mock/mockai-data.json
mock/*.json

# runtime config + secrets (never commit)
worker/.dev.vars
worker/.wrangler/
worker/build/
worker/.gradle/
config/aixodia.json
